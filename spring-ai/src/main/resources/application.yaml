spring:
  ai:
    openai:
      api-key: sk-***
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat

logging:
  level:
    io:
      modelcontextprotocol:
        spec: debug